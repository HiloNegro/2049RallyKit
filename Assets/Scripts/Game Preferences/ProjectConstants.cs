using UnityEngine;
using System.Collections;

public static class ProjectConstants
{
    public enum SceneNames
    {
        [StringValue("Login")]      Login = 1,
        [StringValue("Main")]       Main = 2,
    };
}